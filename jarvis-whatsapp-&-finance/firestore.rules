rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true; // Em ambiente de desenvolvimento/estudo, permitindo acesso total. 
      // Em produção, deve-se restringir ao UID do usuário ou chaves específicas.
    }
  }
}
